<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="com.prateekj"
      android:versionCode="1"
      android:versionName="1.0">
    <uses-sdk android:minSdkVersion="4"/>

    <application android:label="@string/app_name" android:icon="@drawable/ic_launcher">

        <activity android:name=".activity.HomeActivity"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".activity.MyActivity" android:label="@string/pushup_counter">

        </activity>
        <activity android:name=".activity.CreateToDoActivity" android:label="@string/create_todo">

        </activity>

        <activity android:name=".activity.ListToDoActivity" android:label="@string/all_tasks">

        </activity>
        <activity android:name=".activity.TaskActivity" android:label="@string/task">

        </activity>
    </application>
</manifest>
